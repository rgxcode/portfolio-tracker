name: Azure Static Web Apps CI/CD

on:
  push:
    branches:
      - main

jobs:
  build_and_deploy_job:
    runs-on: ubuntu-latest
    name: Build and Deploy Job
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: true
          lfs: false
      - name: Build And Deploy
        id: builddeploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_AMBITIOUS_GLACIER_00EB91F03 }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: "upload"
          app_location: "/frontend" # App source code path
          api_location: "" # Api source code path - optional
          app_build_command: "npm run generate"
          output_location: ".output/public" # Built app content directory - optional
        env:
          NUXT_PUBLIC_ALPHA_VANTAGE_API_KEY: ${{ secrets.NUXT_PUBLIC_ALPHA_VANTAGE_API_KEY }}
          NUXT_PUBLIC_COINGECKO_API_KEY: ${{ secrets.NUXT_PUBLIC_COINGECKO_API_KEY }}
          NUXT_PUBLIC_API_BASE_URL: https://portfolio-tracker-api.azurewebsites.net
